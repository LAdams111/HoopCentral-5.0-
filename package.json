{
  "name": "hoop-central",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "NODE_ENV=production npm run start -w server",
    "start:railway": "npm run db:migrate && npm run db:seed && npm run db:ensure-teams && npm run db:ensure-identities && npm run start",
    "db:ensure-teams": "npm run db:ensure-teams -w server",
    "db:ensure-identities": "npm run db:ensure-identities -w server",
    "db:test-ingest": "npm run db:test-ingest -w server",
    "db:generate": "npm run db:generate -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:push": "npm run db:push -w server",
    "db:seed": "npm run db:seed -w server",
    "db:setup": "npm run db:migrate && npm run db:seed",
    "db:studio": "npm run db:studio -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
