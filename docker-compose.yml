services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hoopcentral
      POSTGRES_PASSWORD: hoopcentral
      POSTGRES_DB: hoopcentral
    volumes:
      - hoopcentral_pg:/var/lib/postgresql/data

volumes:
  hoopcentral_pg:
